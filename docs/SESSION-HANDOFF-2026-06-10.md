# tools-hub - handoff 2026-06-10 (customer-trust copy alignment + production deploy)

Shipped two PRs that close the customer-trust gap on burndown billing. PR #26 inverted the safety_kill billing routing so users get charged for consumed GPU instead of silently refunded (the actual money leak deep-QC pass 9 caught last session). PR #27 aligned the user-facing copy across `/pricing`, `/help/faq`, `/legal/terms` (route `/terms`), the homepage hero, and the job detail cancel UX so what users read before clicking cancel matches what their wallet actually does. Production deploy of `afda37b` verified live on tools.ranomics.com via 4 anonymous curl checks. The 5th anonymous check (boltzgen budget input cap) plus 2 interactive checks (job detail cancel confirm prompt + post-cancel banner Jinja conditional on `gpu_seconds_used > 0`) are deferred because the connected Chrome MCP browser is signed out.

## Done this session

- **PR #26 merged.** `fix(billing): bill safety_kill instead of refunding`. Replaced the unconditional `release_hold` in `shared/jobs.py mid_run_monitor_check` with an inline `_settle_wallet_hold_for_completed_job(fresh)` call mirroring `cancel_job` and `timeout_stuck_job`. Test `tests/test_jobs.py::test_kill_ratio_above_cap_cancels_modal` updated to assert `settle.call_args.kwargs["gpu_seconds"] == 7000.0`, `failure_reason="overrun_safety_kill"`, `release_hold.assert_not_called()`. Merge commit `afda37b`, fix commit `6939c7f`.

- **PR #27 merged.** `fix(billing): align user-facing copy with burn-down failure policy`. Four templates rewritten so the per-cause rule appears identically across surfaces. pricing.html two surfaces (lines 244 to 251 and 318 to 326), faq.html failure FAQ (HTML body and JSON-LD answer) adds safety_kill mention and drops connector hyphens, job_detail.html cancel confirm prompt rewritten and post-cancel banner wrapped in `{% if job.gpu_seconds_used and job.gpu_seconds_used > 0 %}` conditional, legal/terms.html Section 2 enumerates REFUNDED (5 classes) and BILLED-on-failure (3 classes) plus zero-consumption cancel carveout plus "ambiguous defaults to refund". Squash commit `535076a`.

- **Production deploy verified live on `afda37b`.** 4 of 5 anonymous curl checks PASSED. `/help/faq` has both target phrases ("our overrun safety system stopped a runaway job" and "Ambiguous cases default to refund"). `/pricing` Failed jobs card shows the per-cause rule. `/terms` Section 2 enumerates the right buckets. Homepage hero reads "Real scores at the end of the run" with zero remaining "Pilots finish in 15" matches. Railway auto-deploy from main worked.

- **Parallel session already closed the entire deferred queue from the prior new-session prompt.** PR #23 (`20ceb2e`, merged 2026-06-09T23:46Z) shipped 4 items I had queued: docstring drift on `_settle_wallet_hold_for_completed_job` (now enumerates BILLED + REFUNDED + user_cancelled zero-consumption carveout + NULL legacy fallback), defensive clamp `max(0.0, float(job.gpu_seconds_used or 0))` at `shared/jobs.py:812`, regression test `test_unknown_failure_class_falls_through_to_legacy_heuristic` at `tests/test_classifier_settle_routing.py:177`, and dead `_resolve_email_for_user` patch drop from `tests/test_jobs.py` mid-run-monitor tests. Net: zero open code items left.

- **PR #25 design doc for Modal GPU manifest sync.** Documented the two-PR split: PR A on llm-proteinDesigner emits `contracts/gpu_manifest.json` at deploy time via AST walker, PR B on tools-hub reads and overlays TOOL_RULES. No code, no urgency, captured while context was fresh.

- **Repo cleanup.** Local main fast-forwarded from `c002da6` to `afda37b`. Force-deleted squash-merged local feature branches `fix/burndown-billing-copy` and `fix/safety-kill-billing`. Worktree at `C:\Users\lab\Documents\Claude_projects\tools-hub-c2d-g2` confirmed gone (auto-removed earlier). `git worktree list` clean.

## Next steps

- **Resume logged-in UI smoke checks (tasks #60 / #61 / #62)** once you sign into the connected Chrome MCP browser. Three checks pending. Task #60: open `/tools/boltzgen`, confirm the budget `<input>` has `max="50"` and helper text "1 to 50" (the form is behind login so the curl smoke can't see it). Task #61: open any historical job detail page, click Cancel, confirm the JS confirm prompt reads "Cancel this run? Your wallet will be charged for GPU minutes already consumed. Time already used is not refunded." Task #62: on a cancelled job detail page, confirm the banner branches: `gpu_seconds_used > 0` shows "charged for the minutes already used", `gpu_seconds_used == 0 or NULL` shows "full hold refunded".

- **/tools/boltzgen marketing page copy pass for 4 dash violations.** Body prose has 3 em-dashes and 1 connector hyphen that violate the no-dashes rule. Lines surfaced this session: "BoltzGen - structure + affinity design - Boltz-2 binder design" (subtitle), "Boltz-2 binder design - jointly generates" (What it is panel), "main Ranomics site - what the model does" (Learn how panel), "15-60 min per run" (Typical runtime panel). Preexisting, not from this session, but worth a sweep across sibling tool pages (rfdiffusion, rfantibody, bindcraft, pxdesign, mpnn, af2, colabfold, esmfold) for the same pattern.

- **Modal GPU manifest sync implementation** is queued (PR #25 design doc). PR A on llm-proteinDesigner needs to land first to emit `contracts/gpu_manifest.json`; PR B on tools-hub consumes it. Separate session, not blocking anything.

## Blocked / waiting on

- **Logged-in UI smoke checks are blocked on you signing into the connected Chrome MCP browser.** The browser (Browser 1, Windows) is connected via the MCP and reachable but shows "Sign in / Get started" in the navbar at `/tools/boltzgen`. Once you sign in, tasks #60 / #61 / #62 take about 3 minutes total.

## Notes (optional)

- **Customer-trust gap closure cycle complete.** Deep QC pass 9 last session found a real money leak (safety_kill was silently refunding) and 3 copy inconsistencies. This session ran 4 parallel fix agents on Findings 1 / 2 / 3 / 5, ran a fresh QC pass, split into two PRs (PR #26 for the sensitive production change isolating safety_kill from copy review, PR #27 for the four-template copy alignment), and shipped both. Coverage: shared/jobs.py + tests/test_jobs.py for the money fix, four user-facing surfaces for the copy fix, and the deferred queue from the prior session got closed by the parallel chip session in PR #23 between sessions.

- **Boltzgen budget cap rationale (G2 from PR #17).** Form cap is 50 final candidates, even though `num_designs` (the design pool size) stays at 200. The 4x ratio preserves selectivity in top-N ranking. If budget == num_designs the top-N collapses to a no-op and users get no benefit from running boltzgen over a single-shot prediction. The marketing page copy still says "1 to 50 final candidates" inside the form prose; the public landing page reads "5 to 60 min per run and a budget-tunable number of candidates" which is correct.

- **Repo state at handoff.** tools-hub main at `afda37b`. Local branches: only `main` (squash-merged branches deleted). Working tree clean. Stale parent state (per the 2026-06-01 handoff): tools-hub `README.md` + `docs/PRODUCT-PLAN.md` + `supabase/.temp/cli-latest`; llm-pd `.gitignore` + `.planning/phases/11-deployment/11-03-PLAN.md` + `docs/blocker-rfdiffusion.md` + untracked `CLAUDE.md` + `.planning/phases/11-deployment/11-03-PASTE-MANIFEST.md`. Not staged this session either; still flagged.

- **Chrome MCP routing quirk observed.** Calling `navigate(tabId=NEW)` to a new tab routed the navigation to the ORIGINAL tab in the group instead. Two tabs ended up in the group, but the URL change landed on tab `309021177` (the original) rather than `309021181` (the one I created). Worked around by using whichever tabId the URL actually landed on. May be a Chrome MCP bug or an extension permission quirk. Worth a note if anyone else hits it.
