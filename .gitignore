__pycache__/
*.pyc
*.pyo
.env*
!.env.example
venv/
.venv/
*.egg-info/
tmp/
.DS_Store
.pytest_cache/
graphify-out/
.graphify_cached.json
.graphify_uncached.txt
.graphifyignore
scratch/
design-refs/

# Local agent/tooling state (per-machine; never tracked)
.claude/
.gstack/

# Local deploy/run logs (per-run output; never tracked)
.deploy-logs/
runs/
# ...but the compute-campaign UI templates live under templates/runs/ and MUST be tracked.
!templates/runs/
!templates/runs/**

# Proteina direct-call scratch (Modal call id + collected designs)
.proteina_direct_call_state.json
proteina_direct_out/

# Gate 1 boltz2 smoke scratch (Modal call log + parked raw trees). The call
# log is reattach state: tracking it would make a rerun reattach to a dead
# call instead of spawning.
gate1_calls.jsonl
gate1_results*.json
raw_gate1/
raw_selfcheck/
