__pycache__/
*.pyc
*.pyo
.env*
!.env.example
venv/
.venv/
*.egg-info/
tmp/
.DS_Store
.pytest_cache/
graphify-out/
.graphify_cached.json
.graphify_uncached.txt
.graphifyignore
scratch/
design-refs/

# Local agent/tooling state (per-machine; never tracked)
.claude/
.gstack/

# Local deploy/run logs (per-run output; never tracked)
.deploy-logs/
runs/
# ...but the compute-campaign UI templates live under templates/runs/ and MUST be tracked.
!templates/runs/
!templates/runs/**
